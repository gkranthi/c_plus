Printing numbers from 1 to 10


#include <iostream>

using namespace std;

int main() {
    int x;
   cin >>x;
    for (x=1;x<=10;x++)
	cout<<"\n " << x;
}
------------------------------------------------------------------------------------------------------------------------------------------
Swapping of two numbers using two variables.


#include <iostream>

using namespace std;

int main() {
    int x,y;
   cin >>x>>y;

	cout<<"Before swapping x="<<x<<"\ny="<<y;
	 x = (x + y - (y = x));
	 cout<<"\nAfter swapping x="<<x<<"\ny="<<y;
}
------------------------------------------------------------------------------------------------------------------------------------------
write a program using structers.



#include <iostream>

using namespace std;

struct person {
    char name[8];
    int age;
    double salary;
};

 int main()
{
    person p1;
    
    cout << "Enter Full name: ";
    cin.get(p1.name, 8);
    cout << "Enter age: ";
    cin >> p1.age;
    cout << "Enter salary: ";
    cin >> p1.salary;

    cout << "\nDisplaying Information." << endl;
    cout << "Name: " << p1.name << endl;
    cout <<"Age: " << p1.age << endl;
    cout << "Salary: " << p1.salary;
}
